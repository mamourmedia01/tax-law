// Shared types now live with the API client (single source of truth).
export type {
  User,
  Service,
  Review,
  Reveal,
  Provider,
  ProviderDetail,
  Booking,
  BookingStatus,
  Slot,
} from "./lib/api";
